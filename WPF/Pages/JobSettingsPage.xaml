<Page x:Class="WPF.Pages.JobSettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:conv="clr-namespace:WPF.Converter"
      xmlns:infrastructure="clr-namespace:WPF.Infrastructure"
      Title="JobPage"
      Background="#1E1E2E"
      DataContext="{x:Static infrastructure:ViewModelLocator.JobViewModel}">

    <Page.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        
        <Style x:Key="FadeButtonStyle" TargetType="Button">
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="Background" Value="#F9E2AF"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Width" Value="250"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasJobMessage}" Value="True">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 To="1" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 To="0" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>

    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#313244" Padding="20" CornerRadius="10">
            <TextBox Text="{Binding CurrentJob.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="28" Foreground="#CDD6F4" Background="Transparent"
                     BorderThickness="0" HorizontalAlignment="Center" />
        </Border>

        <!-- Body -->
        <StackPanel Grid.Row="1" Margin="0,20" HorizontalAlignment="Center" VerticalAlignment="Top">
            <Button Content="{Binding SourceDirectoryLabel}"
                    ToolTip="{Binding CurrentJob.SourceDirectory}"
                    Click="OnSetSourcePathClick"
                    Width="250" Height="45"
                    Background="#89B4FA" Foreground="#232336" Margin="0,5" />
            <Button Content="{Binding TargetDirectoryLabel}"
                    ToolTip="{Binding CurrentJob.TargetDirectory}"
                    Click="OnSetTargetPathClick"
                    Width="250" Height="45"
                    Background="#89B4FA" Foreground="#232336" Margin="0,5" />

            <ComboBox Width="250" Height="36"
                      ItemsSource="{Binding Source={StaticResource BackupTypeEnum}}"
                      SelectedItem="{Binding CurrentJob.Type, Mode=TwoWay}"
                      Background="#313244"
                      Foreground="#313244"
                      BorderBrush="#A6ADC8"
                      FontSize="16"
                      Margin="0,5" />

            <!-- Encryption UI -->
            <Grid Width="250" Height="30" Margin="0,5">
                <TextBox x:Name="KeyInput"
                         Background="#313244"
                         Foreground="#CDD6F4"
                         BorderBrush="#A6ADC8"
                         FontSize="16" />

                <TextBlock Text="{Binding EncryptionKey, UpdateSourceTrigger=PropertyChanged}"
                           Foreground="#6C7086"
                           Margin="10,0,0,0"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False"
                           Visibility="{Binding Text, ElementName=KeyInput, Converter={StaticResource BoolToVisibility}}" />
            </Grid>
            <Button Content="{Binding EncryptionStatus}" 
                    Command="{Binding ToggleEncryptionCommand}"
                    Width="250" Height="45"
                    Background="#F9E2AF" Foreground="Black" Margin="0,5" />

            <!-- Commands -->
            <Button Content="Run" Command="{Binding RunBackupCommand}" IsEnabled="{Binding CurrentJob.IsReady}"
                    Width="250" Height="45" Background="#A6E3A1" Foreground="Black" Margin="0,5" />
            <Button Content="Delete"
                    Command="{Binding DeleteJobCommand}"
                    CommandParameter="{Binding CurrentJob}"
                    Width="250" Height="45"
                    Background="#F38BA8" Foreground="Black"
                    Margin="0,5" />
            <Button Content="Reset" Command="{Binding ResetJobCommand}" Width="250" Height="45" Background="#89DCEB"
                    Foreground="Black" Margin="0,5" />
        </StackPanel>
    </Grid>
</Page>
