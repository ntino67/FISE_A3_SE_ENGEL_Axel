<Page x:Class="WPF.JobPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="JobPage">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <!-- Hauteur fixe pour la barre du dessus -->
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Encadré du haut avec texte, taille du texte modifiée -->
        <Border Grid.Row="0" Background="#FF2D2D30" Padding="20">
            <TextBlock Text="GESTION DES JOBS"
                       Foreground="White"
                       FontSize="32"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>

        <!-- Contenu principal : boutons empilés verticalement -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Taille du texte des boutons modifiée avec FontSize -->
            <Button Grid.Row="0" Content="Rename job" Margin="0,10" Height="50" Width="200" FontSize="18"/>
            <Button Grid.Row="1" Content="Define source path" Margin="0,10" Height="50" Width="250" FontSize="18"/>
            <Button Grid.Row="2" Content="Define target path" Margin="0,10" Height="50" Width="250" FontSize="18"/>
            <Button Grid.Row="3" Content="Define save mode" Margin="0,10" Height="50" Width="250" FontSize="18"/>
            <Button Grid.Row="4" Content="Encrypt/Decrypt" Margin="0,10" Height="50" Width="250" FontSize="18"/>
            <Button Grid.Row="5" Content="Run" Margin="0,10" Height="50" Width="250" FontSize="18"/>
            <Button Grid.Row="6" Content="Delete" Margin="0,10" Height="50" Width="250" FontSize="18"/>
            <Button Grid.Row="7" Content="Reset" Margin="0,10" Height="50" Width="250" FontSize="18"/>
        </Grid>
    </Grid>
</Page>